match
################################################################################

.. toctree::
    :maxdepth: 1
    :hidden:

    refine.builtin.match.sort_by_alphabet
    refine.builtin.match.sort_by_endswith_match
    refine.builtin.match.sort_by_exact_match
    refine.builtin.match.sort_by_fuzzy_match
    refine.builtin.match.sort_by_initial_words
    refine.builtin.match.sort_by_key_length
    refine.builtin.match.sort_by_last_word
    refine.builtin.match.sort_by_match
    refine.builtin.match.sort_by_startswith_match
    refine.builtin.match.sort_by_word_length
    refine.builtin.match.sort_by_word_match

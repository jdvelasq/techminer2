Run:

vsce package

To install a .vsix file in VS Code:

Open VS Code.
Press Cmd+Shift+P (Mac) or Ctrl+Shift+P (Windows/Linux) to open the Command Palette.
Type and select Extensions: Install from VSIX....
Browse to your .vsix file and select it.
VS Code will install the extension. Reload if prompted.